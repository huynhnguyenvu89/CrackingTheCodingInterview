package LinkedList;

import java.util.HashMap;

/**
 * Write code to remove duplicates from an unsorted linked list.
 */
public class  RemoveDuplicate<T> {

    /**
     * Remove the subsequent duplicates in a given LinkedList. For example:
     * In:      1 -> 1 -> 5 -> 3 -> 5 -> 5 -> 2 -> 9 -> 3
     * Out:     1 -> 5 -> 3 -> 2 -> 9
     *
     * @param input
     */
    public void removeSubsequentDuplicate(LinkedList<T> input){
    }
}
